<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pranavmahajan21.com.viewpagermaterial">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".ParentActivity" />
        <activity android:name=".ZeroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ImageScaleActivity"
            android:label="@string/title_activity_image_scale"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LLWeightActivity" />
        <activity android:name=".TabActivity"></activity>
    </application>

</manifest>